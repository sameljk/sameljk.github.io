<div class="col">
  <a href="{% if switchback.redirect %}{{ switchback.redirect }}{% else %}{{ switchback.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if switchback.img %}
        {%
          include figure.liquid
          loading="eager"
          path=switchback.img
          sizes = "250px"
          alt="switchback thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title">{{ switchback.title }}</h2>
        <p class="card-text">{{ switchback.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if switchback.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ switchback.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if switchback.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ switchback.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
