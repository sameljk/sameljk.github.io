<div class="col">
  <a href="{% if switchbacks.redirect %}{{ switchbacks.redirect }}{% else %}{{ switchback.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if switchbacks.img %}
        {%
          include figure.liquid
          loading="eager"
          path=switchbacks.img
          sizes = "250px"
          alt="switchbacks thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title">{{ switchbacks.title }}</h2>
        <p class="card-text">{{ switchbacks.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if switchbacks.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ switchbacks.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if switchbacks.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ switchbacks.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
